name: Query availability apply run

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/query-availability-apply-run.yml'

permissions:
  contents: write
  actions: read

jobs:
  query:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh run list --workflow retry-p1-availability-contract.yml --limit 5 --json databaseId,status,conclusion,headSha > Metadata/Quality/Availability_Apply_Run.json
      - shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/query-availability-apply-run.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Metadata/Quality/Availability_Apply_Run.json .github/workflows/query-availability-apply-run.yml
          git commit -m 'chore: record availability apply run status'
          git pull --rebase origin main
          git push origin HEAD:main
